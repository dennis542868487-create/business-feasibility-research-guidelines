name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install development dependencies
        run: python -m pip install --requirement requirements-dev.txt
      - name: Regenerate brand assets
        run: python scripts/build_brand_assets.py
      - name: Confirm generated assets are committed
        run: git diff --exit-code -- plugins/business-feasibility-research-guidelines/assets
      - name: Run unit and integration tests
        run: python -m unittest discover -s tests -v
      - name: Validate repository
        run: python scripts/validate_repository.py
      - name: Build release archives
        run: python scripts/build_release_packages.py
      - uses: actions/upload-artifact@v4
        with:
          name: release-packages
          path: |
            releases/*.zip
            releases/checksums.txt
